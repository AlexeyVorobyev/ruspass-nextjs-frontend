query ApolloGetAuthRefresh ($input: TRefreshInput!) {
    auth {
        refresh (input: $input) {
            accessTokenTTL
            accessToken
            refreshTokenTTL
            refreshToken
        }
    }
}